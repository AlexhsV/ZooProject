package com.unipi.ZooForUnipiAlexhs;

public interface AnimalOperations {
    //all animal classes implement this interface, so they can override the method makeNoise, because each animal category
    //makes different noise

    void makeNoise();
}
